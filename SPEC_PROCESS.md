# SPEC_PROCESS.md — 规约与计划生成过程

> 记录与 Superpowers 协作生成 SPEC 和 PLAN 的完整过程。

---

## 一、brainstorming 关键节点

### 1.1 初始意图

我最初的想法是"做一个 Coding Agent 的测试/评测平台"，类似于 SWE-bench 但更灵活。Brainstorming 技能介入后，它逐一追问了我的真实意图：

**智能体追问的 10 个关键问题**（按顺序）：

1. "核心用途是什么？评测基准、编排运行、还是开发调试工具？"
2. "对接什么类型的 Agent？CLI、API、还是自定义 SDK？"
3. "核心工作流是单任务、多 Agent 协作、还是任务队列？"
4. "Agent 执行时需要什么环境？纯沙箱、预配置、还是快照？"
5. "Agent 与仓库的交互方式？Git 工作流、文件直写、还是混合？"
6. "任务定义是什么格式？Issue 驱动、自然语言、结构化 YAML、还是 API？"
7. "Agent 完成后的产出？仅 diff、执行报告、可验证评测、还是完整回放？"
8. "给自己用还是给团队？单机 CLI、内网服务、还是 SaaS？"
9. "技术栈？Python、TypeScript、Go、还是 Rust？"
10. "Agent API 抽象到什么程度？轻量适配器、标准协议、还是插件化？"

### 1.2 关键决策节点

**决策 1：核心理念从"评测平台"转向"编排框架"**

最初我设想的是 SWE-bench 风格的评测工具。Brainstorming 追问后，我意识到核心价值不在"对比多个 Agent 的分数"，而在"让一个 Agent 可靠地完成一个任务"。这个转向决定了后续所有架构选择。

**决策 2：先单机 CLI，预留服务化**

这是一个关键的"YAGNI"决策。Brainstorming 建议了 4 个选项，我选择了"先单机，预留扩展"。这避免了过早引入用户认证、数据库、消息队列等复杂度。

**决策 3：Harness 驱动 Agent Loop**

这是最核心的架构决策。Brainstorming 提出了三种方案：
- A: Harness 驱动（自己实现循环）
- B: Agent 端驱动（利用 API 自带循环）
- C: 混合模式

我选择了 A，因为这与项目要求（§A.4-A "必须自己实现 agent 主循环"）完全一致。这个决策意味着我需要自己管理消息历史、工具调用解析、执行分发、回灌——但这也正是"harness 内核"的价值所在。

### 1.3 可选方案的对比与取舍

| 维度 | 方案 A | 方案 B | 方案 C |
|------|--------|--------|--------|
| Agent Loop | Harness 自己实现 | Agent API 自带 | 混合 |
| 架构 | 核心+插件 | 单进程 Pipeline | 事件驱动+消息总线 |
| 复杂度 | 中等 | 低 | 高 |
| 扩展性 | 好 | 差 | 最好 |
| 符合项目要求 | ✅ 完全符合 | ❌ 不符合 | 部分符合 |

**最终选择：方案 B 的核心+插件架构**，由 Harness 驱动 Agent Loop。

---

## 二、至少 3 轮关键迭代

### 迭代 1：从"评测平台"到"编排框架"

**我的原始想法**：做 SWE-bench 风格的评测工具，跑多个 Agent 对比分数。

**AI 追问**："核心用途是什么？A. 评测基准平台 B. Agent 编排与运行框架 C. Agent 开发调试工具"

**我的修正**：选择了 B（编排框架）。这让我意识到，评测只是编排框架的一个应用场景，而编排框架本身是更基础、更有价值的工程问题。

**修订影响**：SPEC 从"评测系统"转变为"编排框架"，去掉了评分、对比、排行榜等需求，聚焦于"让一个 Agent 可靠地完成一个任务"。

### 迭代 2：Agent 循环由谁驱动

**AI 追问**："工具调用循环由谁驱动？A. Harness 驱动 B. Agent 端驱动 C. 混合"

**我的决策**：选择 A（Harness 驱动）。理由：项目要求"必须自己实现 agent 主循环"（§A.4-A），且 Harness 驱动意味着对循环的完全控制——我可以插入治理护栏、超时控制、取消信号、事件发布。

**我拒绝的方案**：Agent 端驱动（如 OpenAI Assistants API 的 run 模式）。虽然实现更简单，但它把循环逻辑交给了外部 API，无法在循环内部插入护栏和反馈机制。

### 迭代 3：凭据存储方案

**AI 追问**（在设计阶段）："Harness 的交互方式？CLI、Web Dashboard、TUI？"

**我的决策**：先 CLI，后续加 Dashboard。

**后来根据通用要求 §3.1 补充**：凭据必须安全存储。我重新设计了凭据方案：
- 优先级 1：keyring（操作系统级加密）
- 优先级 2：环境变量（明文风险）
- 优先级 3：`.env` 文件（已加入 .gitignore，明文风险已文档化）

**修订影响**：config.yaml 中移除了 `api.openai_api_key` 字段，CLI 新增 `harness credentials` 命令组。

---

## 三、AI 建议的采纳与拒绝

### 采纳的 AI 建议

| 建议 | 来源 | 理由 |
|------|------|------|
| 核心+插件架构 | brainstorming 方案对比 | 模块化好，组件边界清晰，易于测试 |
| 结构化 YAML 任务定义 | 需求追问 | 比自然语言更精确，比 CLI 参数更灵活 |
| 分层报告（摘要/详细/JSON） | 需求追问 | 满足不同场景需求（快速查看 vs 深入分析 vs 工具消费） |
| SQLite + 文件混合存储 | 需求追问 | 轻量元数据（SQLite）+ 大块数据（文件），各取所长 |
| 先异步预留并发 | 需求追问 | 避免过早引入并发复杂度，但架构预留了扩展点 |
| 摘要注入 + 按需获取 | 需求追问 | 平衡 token 消耗和上下文完整性 |

### 拒绝或修正的 AI 建议

| 建议 | 来源 | 拒绝理由 |
|------|------|----------|
| setuptools.backends._legacy:_Backend | plan 代码 | 与 setuptools >= 68.0 不兼容，改为 setuptools.build_meta |
| Agent 端驱动循环 | 方案对比中的选项 B | 不符合项目要求（必须自己实现主循环） |
| 事件驱动+消息总线架构 | 方案对比中的选项 C | 过度设计，对小项目 over-engineering |
| 纯对话模式（无工具调用） | 需求追问 | 没有工具调用的话，Agent 无法真正"操作代码" |

---

## 四、反思

### brainstorming 技能做得好的地方

1. **逐一追问，不跳跃**：每次只问一个问题，每个问题基于前一个答案。这避免了"信息过载"和"需求遗漏"。
2. **提供选项，不开放式**：多数问题以选择题形式呈现，降低了决策负担，同时选项覆盖了合理的设计空间。
3. **方案对比有推荐**：在提出 3 种架构方案时，明确给出了推荐理由，帮助我在相近方案间做出选择。
4. **YAGNI 纪律**：主动建议"先单机，预留扩展"、"先异步，预留并发"，防止过早优化。

### 让我不满的地方

1. **设计文档的"领域与机制设计"部分缺失**：brainstorming 阶段没有引导我深入思考治理护栏、反馈闭环、记忆机制等维度——这些是项目 A 的核心要求，但直到阅读通用要求后才补充。
2. **视觉 companion 未触发**：由于是纯 CLI 项目，brainstorming 从未建议使用视觉 companion。这导致架构图、数据流图等完全依赖文字描述，可读性不如图示。
3. **冷启动验证未在流程中嵌入**：通用要求 §4.5 要求用不同 agent 验证 SPEC+PLAN，但 brainstorming 和 writing-plans 都没有主动提示这一步。我需要在后续手动补做。

---

## 五、冷启动验证（待完成）

按照通用要求 §4.5，需要使用一个**与主开发智能体不同**的 agent，仅凭 SPEC.md + PLAN.md 尝试实现 1-2 个 task。

**计划**：使用 GitHub Copilot CLI 或 Gemini CLI（与 OpenCode 不同），在不提供任何对话历史的前提下，让它尝试实现 Task 2（Task Model）。

**验证结果将记录于此**。

**验证指南**：详见项目根目录下的 `COLD_START_VERIFICATION.md`，包含：
- 推荐的 Agent 选择（Claude Code / Copilot CLI / Cursor / Gemini CLI）
- 标准 prompt 模板（仅提供 SPEC.md + PLAN.md，不提供历史上下文）
- 结果记录模板
- 推荐测试的 Task（Task 2: Task Model 或 Task 5: Adapter Layer）

**验证完成后填写**：

| 项目 | 记录 |
|------|------|
| 使用的 Agent | [待填写] |
| 测试的 Task | [待填写] |
| Agent 暂停提问的位置 | [待填写] |
| 暴露的 spec 缺陷 | [待填写] |
| 错误解读及原因 | [待填写] |
| 产出与预期差距 | [待填写] |
| 对 SPEC/PLAN 的修订 | [待填写] |