services:
  - type: web
    name: coding-agent-harness
    env: python
    runtime: python3.11
    buildCommand: pip install -e .
    startCommand: harness web --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.11